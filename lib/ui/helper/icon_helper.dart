import 'package:flutter/material.dart';

class IconHelper {
  //header
  static const Icon headerCameraIcon = Icon(Icons.camera);
  static const Icon headerSearchIcon = Icon(Icons.search);
  static const Icon headerPopupMenuIcon = Icon(Icons.grain);
  //Body Chats
  static const Icon bodyChatsArchiveBtnIcon = Icon(Icons.archive);
  //Body Status
  static const Icon bodyStatusAddBtnIcon = Icon(Icons.add_circle, size: 50);
  //Body Call
  static const Icon bodyCallMainIcon = Icon(
    Icons.hourglass_bottom,
    size: 40,
  );
  //Body Settins
  //** settings icons are on the text_helper.dart page
}
